import 'package:flutter/material.dart';

class CustomerAppTheme {
  // this is style of title in each of individual screen appbar
  static const TextStyle appBarTitleStyle = TextStyle(
    fontSize: 16.0,
    fontWeight: FontWeight.normal,
  );

  // style for title in each screen - the first line in the screnn
  static const TextStyle title = TextStyle(
    fontSize: 24.0,
    fontWeight: FontWeight.bold,
  );

  // style for subtitle in each screen
  static const TextStyle subtitle = TextStyle(
    fontSize: 16.0,
    fontWeight: FontWeight.w300,
  );

  // button
  static const TextStyle buttonTextStyle = TextStyle(
    fontSize: 16.0,
    fontWeight: FontWeight.bold,
  );

  // list style title
  static const TextStyle listStyleTitle = TextStyle(
    fontSize: 18,
    fontWeight: FontWeight.bold,
  );

  // list style subtitle
  static const TextStyle listStyleSubtitle = TextStyle(
    fontSize: 14.0,
    fontWeight: FontWeight.w300,
  );
}
